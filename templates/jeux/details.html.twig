{% extends 'base.html.twig' %}

{% block title %}Informations sur le jeu{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% include "header.html.twig" %}

<div class="container">
    <!-- Example row of columns -->
    <div class="row">

        <p> {{app.request.get('_route')}} </p>
        
        <p> {{app.request.attributes.get('_route')}} </p>
        <p> {{app.request.pathinfo}}</p>
        <p> {{app.request.uri}}</p>

  
        <div class="col-md-3">
            <h2>{{jeux.title}}</h2>         
            <img src="{{'/' ~ jeux.image}}" alt="" width="150px" height="150px">
            <p> {{jeux.description}} </p>
            <p><a class="btn btn-secondary" href="#" role="button">Noter &raquo;</a></p>
        </div>

        
    </div>

    <hr>

    </div> <!-- /container -->

{% include "footer.html.twig" %}
{% endblock %}
